{{>header}}
	<section>
		<div class="container">
			<div class="row">
				<div class="col-md-12">
					<h1>{{ title }}</h1>
					<p class="mb-0">{{ msg }}</p>
				</div>
			</div>
		</div>
	</section>
{{>footer}}